version: 2

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"
  apt_packages:
    - xvfb
  jobs:
    pre_build:
      - bash docs/start_xvfb.sh
